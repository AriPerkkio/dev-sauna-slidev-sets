{
  "name": "dev-sauna",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "slidev",
    "build": "slidev build",
    "preview": "npx vite preview",
    "deploy": "touch dist/.nojekyll && gh-pages --dist dist --dotfiles true",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "typescript": "^5.0.3"
  },
  "dependencies": {
    "@slidev/cli": "^0.41.1",
    "@slidev/theme-default": "^0.21.2",
    "example-types": "*"
  },
  "pnpm": {
    "overrides": {
      "example-types": "link:./types"
    }
  }
}
